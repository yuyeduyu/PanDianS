package com.anye.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ascend.assetcheck_jinhua.dao.CompletePlace;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "COMPLETE_PLACE".
*/
public class CompletePlaceDao extends AbstractDao<CompletePlace, Long> {

    public static final String TABLENAME = "COMPLETE_PLACE";

    /**
     * Properties of entity CompletePlace.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property TaskId = new Property(1, int.class, "taskId", false, "TASK_ID");
        public final static Property ReceivePlace = new Property(2, String.class, "receivePlace", false, "RECEIVE_PLACE");
    };


    public CompletePlaceDao(DaoConfig config) {
        super(config);
    }
    
    public CompletePlaceDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"COMPLETE_PLACE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"TASK_ID\" INTEGER NOT NULL ," + // 1: taskId
                "\"RECEIVE_PLACE\" TEXT);"); // 2: receivePlace
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"COMPLETE_PLACE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CompletePlace entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getTaskId());
 
        String receivePlace = entity.getReceivePlace();
        if (receivePlace != null) {
            stmt.bindString(3, receivePlace);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CompletePlace entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getTaskId());
 
        String receivePlace = entity.getReceivePlace();
        if (receivePlace != null) {
            stmt.bindString(3, receivePlace);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CompletePlace readEntity(Cursor cursor, int offset) {
        CompletePlace entity = new CompletePlace( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // taskId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // receivePlace
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CompletePlace entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTaskId(cursor.getInt(offset + 1));
        entity.setReceivePlace(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CompletePlace entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CompletePlace entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
